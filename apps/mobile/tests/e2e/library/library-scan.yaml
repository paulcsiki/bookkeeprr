# M5a Library group — Library Scan flow.
# AUTHORED ONLY (device-run optional): no emulator is wired into this checkout,
# so this flow is committed for the Maestro suite but not yet exercised on a
# device. Library Scan is admin-gated, so it relies on the AUTOAUTH admin
# session from the connected onboarding flow.
appId: com.bookkeeprr.app
env:
  EXPO_PUBLIC_MOBILE_E2E: '1'
  EXPO_PUBLIC_MOBILE_E2E_AUTOAUTH: '1'
---
- launchApp: { clearState: true }
- runFlow: { file: ../onboarding/connected.yaml }
- tapOn: { id: 'btn-go-library' }
- tapOn: { text: 'Settings' }
- scrollUntilVisible:
    element: { id: 'row-library-scan' }
    centerElement: true
- tapOn: { id: 'row-library-scan' }
- assertVisible: { id: 'screen-library-scan' }
