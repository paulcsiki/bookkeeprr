// foliate-js ships no type declarations. Importing `foliate-js/view.js`
// registers the `<foliate-view>` custom element (side-effect import); the
// component narrows the element to its own minimal `FoliateView` surface.
declare module 'foliate-js/view.js';
