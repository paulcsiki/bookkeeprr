/**
 * Single display version for the marketing site. The topbar pill, the footer,
 * and the Docker example all read this constant, so they always show the same
 * version. Bump it together with apps/website/package.json on a release.
 */
export const APP_VERSION = '1.0.0';
