# @bookkeeprr/logic

Pure helpers shared across bookkeeprr apps. RN-safe (no DOM, no Node-only APIs).

## Exports

```ts
openLibraryCoverUrl(isbn, size?, { default? })
formatBytes(bytes, fractionDigits?)
formatDuration(ms)
```
